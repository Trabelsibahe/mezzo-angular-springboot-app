import { KeyValue } from "@angular/common";
import { ResolveStart, RouterLink } from "@angular/router";

export class News {
  id: number=0;
  title !:string;
  description !:string;
  image !:string;
}